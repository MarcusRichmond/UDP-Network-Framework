using UnityEngine;


//Just using this class to tell the server were the players should spawn.
//Its attactched to a gameObject and the gameNetworkHandler script find them all and gets there Transform so it can use there location.
public class NetworkSpawnLocation : MonoBehaviour {

}
